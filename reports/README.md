# Reports

`deep-research/` contains generated `.docx` research reports used to define source strategy, rights strategy, vocabulary normalization, field mapping, interface structure, and capture scope.

These reports are retained as project evidence and methodology history. Operational rules derived from them are summarized in `docs/`.
