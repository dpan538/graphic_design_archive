import BookmarkLab from "@/components/archive/bookmarks/BookmarkLab";

export const metadata = { title: "Bookmark layout studies — Archive Box" };

export default function BookmarksPage() {
  return <BookmarkLab />;
}
