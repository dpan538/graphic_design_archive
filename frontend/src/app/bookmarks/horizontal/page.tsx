import BookmarkLab from "@/components/archive/bookmarks/BookmarkLab";

export const metadata = { title: "Horizontal bookmark studies — Archive Box" };

export default function HorizontalBookmarksPage() {
  return <BookmarkLab mode="horizontal" />;
}
