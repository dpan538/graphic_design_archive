import BookmarkLab from "@/components/archive/bookmarks/BookmarkLab";

export const metadata = { title: "Vertical bookmark studies — Archive Box" };

export default function VerticalBookmarksPage() {
  return <BookmarkLab mode="vertical" />;
}
