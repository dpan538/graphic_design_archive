import AppendixLab from "@/components/archive/appendix/AppendixLab";

export const metadata = { title: "Appendix asset studies — Archive Box" };

export default function AppendixPage() {
  return <AppendixLab />;
}
