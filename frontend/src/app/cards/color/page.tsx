import CardLab from "@/components/archive/cards/CardLab";

export const metadata = { title: "Color card studies — Archive Box" };

export default function ColorCardsPage() {
  return <CardLab mode="color" />;
}
