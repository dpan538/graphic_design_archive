import CardLab from "@/components/archive/cards/CardLab";

export const metadata = { title: "Special proportion card studies — Archive Box" };

export default function SpecialCardsPage() {
  return <CardLab mode="special" />;
}
