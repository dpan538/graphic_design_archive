import CardLab from "@/components/archive/cards/CardLab";

export const metadata = {
  title: "High capacity card studies — Archive Box",
};

export default function DenseCardsPage() {
  return <CardLab mode="dense" />;
}
