# TypeScript adapter conformance

The adapter checks the shared schema surface and schema-aware canonical serialization. 14 fixtures cover freeze, seed, tree, instance, unknown field, duplicate identity, dangling reference, contamination, origin, and hash rejection.
