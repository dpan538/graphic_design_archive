# Active reference scan

The deterministic scanner covers the pure Exploration domain, any future `trace-v49/exploration` feature directory, frontend manifests, legacy execution-directory existence, package commands, public Exploration route/API paths, sealed evidence diffs, and protected Search/Context/Spacetime/database diffs.

Result: `PASS` with zero active implementation violations.

Policy/validator denial lists and adversarial fixtures are classified as enforcement, not runtime capability. Sealed Round 6/7 research and audit paths are classified as superseded historical evidence. Search is intentionally outside semantic-term scanning and is protected by source-scoped diff instead.

The authoritative scan table is `docs/research/trace-v49-exploration-conceptual-reset/09_ACTIVE_PATH_FORBIDDEN_REFERENCE_SCAN.tsv`.
