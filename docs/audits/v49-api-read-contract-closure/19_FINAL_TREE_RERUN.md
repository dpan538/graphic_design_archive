# Final-tree rerun

The complete verification tree was committed as `f5e52545f9cc9e125f095039df5a636d480bdb36`. From an empty candidate-owned database, the formal runner reproduced all core fingerprints and the final sequence passed:

1. FRESH_C formal replay and official verifier;
2. current-leaf, 14/14 missingness, and 36/36 DML permission matrices;
3. sealed/current disposable release with 7,995 public surfaces;
4. exhaustive 18/18 API templates, 170 contract cases, 72/72 negative methods, 49 search cases, and 558/558 runtime requests;
5. stable-ID/release/database crosschecks, statistics, schema hash, typecheck, repository/module contracts, 22-vector parity, OpenAPI parse, and production build;
6. clean shutdown of the only task-owned PostgreSQL cluster and residual process count zero.

Only audit documents, generated evidence, examples, and statistics changed after that candidate commit. The final audit-only commit is therefore verified with the minimum semantic suite: typecheck, read-platform contract, production-path `pageByKey` module import, 22-vector adapter parity, OpenAPI parse, production build, audit manifest/checksum verification, changed-path classification, and Git cleanliness. The exact final local/remote/execution SHA is reported in the external final receipt because a commit cannot contain its own hash.
