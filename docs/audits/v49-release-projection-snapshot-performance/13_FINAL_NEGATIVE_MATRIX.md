`FINAL_TREE_FOCUSED_CURRENT_LEAF_TEST=PASS`

The final-tree focused test covers current-leaf selection, held exclusion,
unknown fault SQLSTATE, candidate validation and seal.  It does not replace
the required full final matrix.

`FAILURE_INJECTION_PASS_COUNT=UNVERIFIED`
`MISSINGNESS_NEGATIVE_CASES=UNVERIFIED`
`POST_BUILD_DML_DENIAL_CASES=UNVERIFIED`
`CONCURRENCY_MATRIX=UNVERIFIED`

These gates are deliberately not inherited as PASS from v3/v4 evidence.
