# Pair-matrix validation

The ordered matrix has exactly 256 unique cells and no undecided value. Counts: `{'DEFER_DIRECTIONALITY': 2, 'DEFER_SINGLE_ATTESTATION': 1, 'REJECT_SELF_RELATION': 16, 'UNSUPPORTED_DEFAULT_DENY': 237}`. All 16 diagonals are explicitly decided. Unsupported pairs default deny. No pair passes, so no co-occurrence-only, same-issue-only, or single-author passing rule exists.
