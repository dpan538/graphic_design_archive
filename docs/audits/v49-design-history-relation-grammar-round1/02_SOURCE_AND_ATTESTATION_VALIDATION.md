# Source and attestation validation

The grammar registry contains 28 metadata-verified scholarly sources and 30 bounded attestations. Every passing Node has at least one new grammar source. Contexts are bounded; evidence hashes are present; source languages and translations are recorded. No passing pair exists, so the two-attestation failure count for passing rules is zero by construction, not waiver.
