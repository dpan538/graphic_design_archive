# Round 16A goal and method

Round 16A tests the complete finite governed Function 3 space at immutable source `8de5d1dedffc6fd70d8b03cd63fdec74c0d40f6e`. It does not perform a Search, Context Canvas, Spacetime, frontend-design, or deployment round.

The closure pipeline is deterministic except for the timestamped external scholarly-discovery stage:

1. reconcile the authority and architecture;
2. freeze the deduplicated all-disposition vocabulary-candidate universe;
3. give every candidate one final disposition and freeze active vocabulary;
4. generate every unordered pair of active terms;
5. run the same evidence and qualification workflow for every pair;
6. build and statistically describe the complete validated undirected graph;
7. freeze every finite parameter domain;
8. enumerate every connected supported edge-subgraph within two to eight nodes;
9. pass each candidate through a versioned adapter around the unchanged Round 15 engine and evaluate every topology family;
10. enumerate seed and category-entry variants without conflating their identities;
11. generate all reachable states and execute the complete reducer-derived transition relation;
12. derive canonical shortest workflows and replay each twice;
13. render and validate every state/preset/theme export variant;
14. prove equivalence between the full audit census and compact production model;
15. test the built production server over HTTP, including concurrency and sustained workloads;
16. independently recompute the headline counts and reproduce deterministic artifacts in an isolated clean worktree.

No expected legacy count is a stopping rule. Enumeration is streamed to ledgers and partitioned deterministically where necessary. Any unresolved census row, failed reconciliation equation, incomplete export, failed production workload, or reproduction mismatch forces `TRACE_FUNCTION3_EXPLORATION_CLOSED=false`.

Timing, memory, CPU, temporary storage, final audit bytes, production-model bytes, and HTTP response bytes are measured separately. In-process service calls are labelled microbenchmarks and cannot substitute for production HTTP results.

The final decision distinguishes computational/backend closure from external academic endorsement. External human review is not simulated and remains false.

