# Local higher-order candidate-universe census

## Result and semantic boundary

The first executable local-selector tranche found 348 governed trigger occurrences and collapsed their exact resolved participant sets into 31 review families. These 31 records are an initial local lower bound, not 31 validated associations and not a closed candidate universe. Every family is `DISCOVERED`, its evidence review is `NOT_STARTED`, its global-coherence review is `NOT_REVIEWED`, and its product eligibility fails closed.

The family arity distribution is 21 three-member, 4 four-member, 1 five-member, 4 six-member, and 1 eight-member review families. Three are control-only families containing rejected participants. The census uses 43 distinct governed senses: 30 active, 6 research-only, and 7 rejected. A review family may later split into several case- or scope-specific candidates, merge with another family, become a control or exclusion, or fail review. Its provisional participant-set key is therefore not yet a governed association identity.

The trigger-occurrence distribution is:

| Trigger surface | Occurrences |
| --- | ---: |
| Round 16A production compositions | 186 |
| Round 16A topology compositions | 60 |
| Round 16A connected subgraphs | 37 |
| Round 15 composition fixtures | 24 |
| Round 14 active-pair shared-locus bundles | 10 |
| Round 14 n-ary fixtures | 6 |
| Round 14 concept-only higher-order lead | 1 |
| Round 13 direct positive-field passages | 6 |
| Round 13 incidental case-label control | 1 |
| Round 14 exact archive-context duplicates | 4 |
| Round 16 legacy compositions | 4 |
| Round 10 direct passages | 3 |
| Round 10 explicit cluster near-misses | 2 |
| Round 9 context collision, Round 13 qualification extension, Round 14 qualified overlap, and Round 16 vocabulary-only structure | 1 each |

The 319 structural occurrences reduce to 20 distinct participant-set families. Local passage, locus, collision, overlap, and vocabulary triggers add eleven distinct families. Deduplication here means only that the canonical participant sets match; it does not claim that their cases, roles, order, scope, or evidence are equivalent. The final pre-commit selector audit added `COMP-EVID-011` as a distinct locator occurrence, retained the rejected “Brazilian exposition” case label in `COMP-EVID-003` as an explicit control rather than silently ignoring it, and recovered a cultural-mobility / mobile-object / mediation lead that an enclosing hard-negative pair review had obscured.

## Cross-round concept-sense resolution

`raw/concept-sense-crosswalk-v1.tsv` resolves all 65 frozen Round 16A vocabulary candidates into stable Round 16B participant-sense identifiers. It preserves 31 active, 21 research-only, 12 rejected, and 1 merged/superseded disposition. Active rows retain their `TRV:` identity, while non-active candidates remain distinct research senses and are never silently promoted.

This populated versioned crosswalk supersedes the empty method-checkpoint template `raw/concept-sense-crosswalk.tsv` for subsequent census work. Likewise, `raw/candidate-trigger-occurrence-ledger-v1.tsv` supersedes the empty occurrence template. The original templates remain immutable evidence of checkpoint 002 and retain their recorded hashes.

Ten Round 10 n-ary argument-role records still lack closed governed participant sets. They remain in `raw/open-participant-resolution-ledger-v1.tsv` and were not converted into concepts or emitted as candidate families. The independent verifier proves the source set is exact.

## Isolated active vocabulary

The five active terms isolated in the Round 16A pair graph are canonization, cultural transfer, cultural transformation, mobile object, and self-exoticization. Four occur in at least one local review family; that occurrence does not prove higher-order composability. Self-exoticization does not occur in any of the 31 families. All five remain open in `raw/isolated-active-term-audit-ledger-v1.tsv`, with no product-accessibility decision inferred.

## Prior-object no-loss universe

The selected object-level universe records 32,135 row-exact reconciliation obligations across 43 sets: fixtures, vocabulary and association representations, semantic images, compositions, topologies, category representations, source-inventory entries, failed-association results, state/hash indexes, transition descriptors, capability and database-authority fields, seeds, states, workflows, exports, enumeration results, rejections, and legacy-reconciliation decisions. These are strong commitments for the selected representations; they are not a claim that every field in every prior artifact has already received a row-level semantic disposition. The 749,944-row Round 16A transition census is not duplicated; its frozen source hash, exact count, sorted transition-ID hash, and 228 per-production descendant partitions preserve an independently verifiable set commitment.

A second, complete file-level conservation manifest binds all 1,464 tracked files under 16 declared Round 15, Round 16, and Round 16A audit, research, generator, schema, read-model, runtime, and API namespaces to their exact Git blob OIDs, modes, sizes, source SHA, and source tree. Sixteen paths also have selected row-exact object-set coverage. The other 1,448 files remain `FILE_BOUND_OBJECT_POLICY_RECONCILIATION_PENDING`: they cannot disappear or change silently, but file binding does not substitute for later object-policy and semantic review.

No prior object has yet been semantically carried forward. Every row remains pending a governed higher-order or object-policy reconciliation. This prevents an earlier product result from disappearing, becoming group evidence, or remaining active by compatibility alone.

The initial generator emitted a 27.2 MB monolithic ordinary-Git ledger, crossing the round's 25 MB warning threshold. Before commit, the ledger was replaced with deterministic core, state, workflow, and export shards. Later independent no-loss audits found that several v1 representation sets, the 11 v2 legacy-reconciliation decisions, and the analogous public v2 representation and capability-policy sets were not in the selected row-exact universe. Those selected commitments were added. A further audit correctly found that 43 selected sets still could not support a claim about every prior artifact, so the complete 1,464-file source-tree conservation layer was added and the claim narrowed. Every corrective rerun remains in the execution record. Each generated shard is below the warning threshold, and no history was rewritten.

## Verification and remaining work

`scripts/trace_round16b/verify_local_candidate_census.py` independently reconstructs and checks the crosswalk, input surfaces, occurrence identities, exact structural source sets, local family set and arity, open queues, isolated terms, selected prior-object commitments, the complete source-tree artifact manifest, topology and product invariants, all 749,944 transitions, and 228 descendant partitions. The versioned verification receipt records the exact passing check count; the verifier does not import the generator.

The execution input manifest has 34 exact hash-and-size-bound inputs. It adds the Round 16A production read model, which the method inventory did not list separately even though its structures were required by the trigger contract, plus the method inventory itself. Of the method inventory's 44 surfaces, 22 are selected execution inputs and the Round 10 observed-chain registry is separately inspected. Its two rows each bind exactly two terms, are inactive, forbid transitive inference, and therefore emit zero higher-order candidates. The remaining 21 surfaces are hash-accounted but explicitly deferred with individual reasons and next actions in `raw/local-surface-disposition-ledger-v1.tsv`. This means local selector completeness remains open. The Round 13 vocabulary-gap mirror is excluded from double counting because it repeats the governing evidence registry.

This checkpoint does not complete local evidence review, source-rights review, database discovery, adaptive external scholarly search, falsification review, candidate scope/case splitting, exclusions, global coherence, product reconciliation, model/API work, or closure. All six closure dimensions remain false.
