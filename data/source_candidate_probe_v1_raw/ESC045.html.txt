<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>National Library Portal</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --page-blue: #163c82;
      --overlay-blue: rgba(20, 55, 120, 0.79);
      --overlay-deep: rgba(8, 20, 48, 0.68);
      --gold: #d5a436;
      --white: #ffffff;
      --card-bg: rgba(8, 15, 31, 0.84);
      --card-border: rgba(255, 255, 255, 0.08);
      --card-shadow: 0 18px 40px rgba(3, 9, 24, 0.32);
      --max-width: 1220px;
    }

    body {
      min-height: 100vh;
      font-family: 'Inter', sans-serif;
      color: var(--white);
      background: #0e2452;
      overflow-x: hidden;
    }

    .landing {
      position: relative;
      min-height: 100vh;
      isolation: isolate;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px 24px;
      overflow: hidden;
    }

    .landing-bg,
    .landing-overlay {
      position: absolute;
      inset: 0;
      pointer-events: none;
    }

    .landing-bg {
      background: url('src/background.jpg') center center / cover no-repeat;
      transform: scale(1.01);
      opacity: 1;
      z-index: -3;
    }

    .landing-overlay {
      background:
        linear-gradient(180deg, rgba(17, 48, 104, 0.42), rgba(12, 33, 76, 0.52)),
        linear-gradient(120deg, rgba(19, 56, 123, 0.36), rgba(7, 19, 46, 0.44)),
        radial-gradient(circle at top center, rgba(255, 255, 255, 0.07), transparent 34%);
      z-index: -2;
    }

    .hero-shell {
      width: min(100%, var(--max-width));
      display: grid;
      gap: 40px;
      justify-items: center;
      text-align: center;
      padding: 24px 0;
    }

    .hero-copy {
      display: grid;
      gap: 14px;
      justify-items: center;
      animation: rise-in 0.8s ease-out both;
    }

    .welcome-line {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      align-items: baseline;
      gap: 18px;
      font-family: 'Poppins', sans-serif;
      font-size: clamp(1.9rem, 3.5vw, 3.2rem);
      font-weight: 700;
      line-height: 1.1;
      letter-spacing: -0.03em;
      text-shadow: 0 10px 24px rgba(0, 0, 0, 0.22);
    }

    .accent-divider {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      margin-top: 2px;
    }

    .accent-divider::before,
    .accent-divider::after {
      content: '';
      width: clamp(72px, 11vw, 110px);
      height: 4px;
      border-radius: 999px;
      background: var(--gold);
    }

    .accent-divider span {
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: var(--gold);
    }

    .identity-row {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      max-width: 100%;
      padding: 0 12px;
    }

    .identity-mark {
      width: 100px;
      height: 36px;
      flex: 0 0 auto;
      object-fit: contain;
      filter: drop-shadow(0 8px 12px rgba(0, 0, 0, 0.18));
    }

    .identity-text {
      font-size: clamp(0.9rem, 1.2vw, 1rem);
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
      color: rgba(255, 255, 255, 0.92);
    }

    .portal-grid {
      width: 100%;
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 14px;
      animation: rise-in 0.95s ease-out both 0.12s;
    }

    .portal-card {
      position: relative;
      min-height: 180px;
      background: var(--card-bg);
      border: 1px solid var(--card-border);
      border-radius: 0 38px 0 18px;
      padding: 22px 18px 18px;
      box-shadow: var(--card-shadow);
      backdrop-filter: blur(4px);
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      overflow: hidden;
      text-align: left;
      text-decoration: none;
      color: inherit;
      transition: transform 180ms ease, background 180ms ease, border-color 180ms ease;
    }

    .portal-card::before {
      content: '';
      position: absolute;
      inset: auto -18% -40% 45%;
      height: 120px;
      background: radial-gradient(circle, rgba(213, 164, 54, 0.16), transparent 65%);
      pointer-events: none;
    }

    .portal-card:hover {
      transform: translateY(-4px);
      background: rgba(9, 18, 37, 0.92);
      border-color: rgba(255, 255, 255, 0.14);
    }

    .portal-title {
      font-family: 'Poppins', sans-serif;
      font-size: clamp(1rem, 1.28vw, 1.32rem);
      font-weight: 700;
      line-height: 1.1;
      max-width: 16ch;
    }

    .portal-link {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      align-self: flex-end;
      width: 44px;
      height: 44px;
      background: url('src/chevron-direction-right-round-filled-white-icon.webp') center center / contain no-repeat;
      filter: drop-shadow(0 10px 24px rgba(0, 0, 0, 0.22));
    }

    @keyframes rise-in {
      from {
        opacity: 0;
        transform: translateY(24px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 860px) {
      .portal-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

@media (max-width: 560px) {
  .landing {
    padding: 28px 18px;
  }

  .hero-shell {
    gap: 30px;
  }

  .welcome-line {
    gap: 10px;
  }

  .identity-row {
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 14px;
  }

  .identity-mark {
    width: 90px;
    height: auto;
  }

  .identity-text {
    line-height: 1.5;
    font-size: 0.95rem;
  }

  .portal-grid {
    grid-template-columns: 1fr;
  }

  .portal-card {
    min-height: 150px;
  }
}
  </style>
</head>
<body>

<main class="landing">
  <div class="landing-bg" aria-hidden="true"></div>
  <div class="landing-overlay" aria-hidden="true"></div>
  <section class="hero-shell" aria-label="National Library portal links">
    <div class="hero-copy">
      <h1 class="welcome-line">ආයුබෝවන් <span>வணக்கம்</span> <span>Welcome</span></h1>
      <div class="accent-divider" aria-hidden="true">
        <span></span>
      </div> <br>
        <img class="identity-mark" style="width: 125px;" src="src/NLSL Logo (white).png" alt="National Library logo" />

      <p class="identity-text" style="font-size: 19px;">ජාතික පුස්තකාල හා ප්‍රලේඛන සේවා මණ්ඩලය</p>
      <p class="identity-text"style="margin-top: -8px;">தேசிய நூலக ஆவணவாக்கல் சேவைகள் சபை</p>
      <p class="identity-text"style="margin-top: -3px;">National Library and Documentation Services Board</p>
    </div>

    <div class="portal-grid">
      <a class="portal-card" href="https://www.natlib.lk/NLDSB" target="_blank" rel="noopener noreferrer">
        <h2 class="portal-title">National Library and Documentation Services Board</h2>
        <span class="portal-link" aria-hidden="true"></span>
      </a>

      <a class="portal-card" href="https://www.natlib.lk/NLSL" target="_blank" rel="noopener noreferrer">
        <h2 class="portal-title">National Library of Sri Lanka</h2>
        <span class="portal-link" aria-hidden="true"></span>
      </a>

      <a class="portal-card" href="https://www.natlib.lk/ICNATLIB2026" target="_blank" rel="noopener noreferrer">
        <h2 class="portal-title">Annual Research Conference</h2>
        <span class="portal-link" aria-hidden="true"></span>
      </a>

      <a class="portal-card" href="https://isbn.lk/" target="_blank" rel="noopener noreferrer">
        <h2 class="portal-title">ISBN/ISSN/ISMN</h2>
        <span class="portal-link" aria-hidden="true"></span>
      </a>
    </div>
  </section>
</main>
</body>
</html>
